# Crypto Volatility Prediction - Environment Variables
# Copy to .env: cp .env.example .env
#
# These values match the defaults in compose.yaml and config.yaml.
# Modify if running services on different hosts/ports.

# Kafka (from compose.yaml)
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# MLflow (from compose.yaml)
MLFLOW_TRACKING_URI=http://localhost:5000

# Coinbase WebSocket (public API, no auth required)
COINBASE_WS_URL=wss://ws-feed.exchange.coinbase.com
